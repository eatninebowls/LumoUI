import Progress from './Progress.vue'
import { withInstall } from '../../utils/withInstall'

export const LumoProgress=withInstall(Progress,'LumoProgress')
export default LumoProgress
export type{ProgressProps} from './types'
